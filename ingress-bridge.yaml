apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kafka-bridge-ingress
spec:
  rules:
      - http:
          paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: strimzi-kafka-bridge-bridge-service
                port:
                  number: 8080   


